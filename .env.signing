# Apple Developer署名設定
# 作成日: $(date)

# Developer ID Installer証明書（PKG署名用）
# 証明書インストール後、以下のコマンドで正確な名前を確認:
# security find-identity -p basic -v | grep "Developer ID Installer"
export DEVELOPER_ID="Developer ID Installer: Yuki Hamada (5BV85JW8US)"

# Apple ID (開発者アカウントのメールアドレス)
# Apple Developerにログインする時のメールアドレス
# CSR作成時のメールアドレス欄にも同じものを使用
export APPLE_ID="mail@yukihamada.jp"

# Team ID (確認済み)
export APPLE_TEAM_ID="5BV85JW8US"

# App-specific password
# https://appleid.apple.com/account/manage → セキュリティ → App用パスワード
# 生成後、以下の形式で設定: xxxx-xxxx-xxxx-xxxx
export APP_PASSWORD=""